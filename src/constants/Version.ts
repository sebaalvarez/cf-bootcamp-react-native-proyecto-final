// Versión actual de la aplicación
// Debe coincidir con la versión en package.json y en build.gradle
export const APP_VERSION = "1.0.1";
export const APP_VERSION_CODE = 2;

// Incrementar VERSION_CODE en cada release
// VERSION_CODE debe ser un número entero incremental: 1, 2, 3, etc.
